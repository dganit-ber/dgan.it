export default function Footer() {
  return (
    <footer className="relative border-t border-border px-6 py-8 text-center text-sm text-muted">
      <div className="absolute inset-x-0 -top-px h-px bg-linear-to-r from-transparent via-accent/50 to-transparent" />
      <p className="animate-fade-in">Placeholder content — to be written.</p>
    </footer>
  );
}
